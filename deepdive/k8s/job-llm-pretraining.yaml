apiVersion: batch/v1
kind: Job
metadata:
  name: llm-pretraining-job
  namespace: deepdive
spec:
  template:
    spec:
      containers:
        - name: pretraining
          image: localhost:32000/llm/pretraining/llm-data-pretraining:0.2.1
          envFrom:
            - configMapRef:
                name: llm-pretraining-config
          volumeMounts:
            - name: data-volume
              mountPath: /opt/llm-data-pretraining/data
          resources:
            requests:
              cpu: "2"
              memory: "4Gi"
            limits:
              cpu: "4"
              memory: "8Gi"
      restartPolicy: OnFailure
      securityContext:
        fsGroup: 1000 # Adjust if the Dockerfile appuser has a different entry
      volumes:
        - name: data-volume
          persistentVolumeClaim:
            claimName: llm-data-pvc
  backoffLimit: 4
